#include "SST.h"


// TODO: add error checking for when key is not found
long SST::search(long key) {
  // TODO: Binary search on SST at pilepath

  // TODO: Read size of file

  // TODO: Number of blocks = size of file / BLOCK_SIZE

  // TODO: Read Middle block

  // TODO: Look at individual block
  //          if first entry <= Key <= last entry, then key is inside our block and we are done search
  //          else if k > last entry, do binary search on all blocks to the right
  //          else if k < first entry, do binary search on all blocks to the left

  
}